# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# relay runtime history log (grows; regenerated at runtime)
relay/data/
# explicit: telegram bot secret + subscriber store must never be committed (covered by relay/data/ too)
relay/data/telegram.token
relay/data/telegram-subs.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# agent config
CLAUDE.md
AGENTS.md
.claude/

# Working documents. Untracked on purpose: a visibility flip exposes every commit
# ever made, so a doc deleted from HEAD is still public. README.md is the project.
docs/

# Working documents. Notes to myself, not documentation. A visibility flip
# exposes every commit ever made, so a doc deleted from HEAD is still public.
# docs/ alone was not enough: these four names live at the repo root.
docs/
/NORTH_STAR.md
/PROJECT_STATE.md
/ROADMAP.md
/BACKLOG.md
/checkpoints/
/plans/
/internal/
